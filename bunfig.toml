[install]
# Use exact versions for reproducible builds
exact = true

[run]
# Enable hot reloading during development
hot = true

[build]
# Target Bun runtime for production builds
target = "bun"

# Optimize for speed
minify = true
